class `85 Maximal Rectangle` {



    fun maximalRectangle(matrix: Array<CharArray>): Int {
        if(matrix.isEmpty() || matrix[0].isEmpty()) return 0
        val dp = Array(matrix.size) { IntArray(matrix[0].size) }
        TODO()
    }
}